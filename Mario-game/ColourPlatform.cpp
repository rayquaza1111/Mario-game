#include "ColourPlatform.h"

//void CColourPlatform::GetBoundingBox(float& l, float& t, float& r, float& b)
//{
//	l = (x - cellWidth / 2);
//	t = (y - cellHeight / 2);
//	r = l + cellWidth * length;
//	b = t + cellHeight;
//	//RenderBoundingBox();
//}
